# Backup — original site (pre-redesign)

Untouched copies of every page as it existed at commit `96081e3`, before the
homepage was rebuilt as a single hero.

- `index.html` — the original multi-section homepage
- `about.html`, `pricing.html`, `templates.html`, `contact-options.html`

Kept for reference while the remaining pages are brought into the new design.
Excluded from search engines via `/robots.txt`. Safe to delete once the content
has been carried over.
